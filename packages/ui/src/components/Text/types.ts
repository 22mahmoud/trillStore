export interface ITextProps {
  bold: boolean;
  size: number;
  uppercase: boolean;
  lowercase: boolean;
  capitalize: boolean;
  color: string;
  as: React.ReactType;
}

export interface ITextProps {
  children: any;
}
